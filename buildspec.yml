version: 0.2
phases:
    pre_build:
      commands:
        - echo "Entered prebuild "
    build:
        commands:
          - echo "Entered build s"
          - mvn clean install

artifacts:
  files:
    - target/test-study-group-aws-1.0-SNAPSHOT.jar
